## Testdurchführung

Die API-Endpunkte wurden direkt im Browser getestet.

Getestete Endpunkte:

- `/api/items`
- `/api/requests`

Beide Endpunkte lieferten die erwarteten JSON-Daten erfolgreich zurück.

## Testergebnis

Die API funktioniert wie erwartet. Die JSON-Antworten enthalten strukturierte Daten und können direkt im Browser angezeigt werden.

## Fazit

Die Tests bestätigen, dass die API-Endpunkte korrekt arbeiten und die Daten erfolgreich im JSON-Format bereitgestellt werden.